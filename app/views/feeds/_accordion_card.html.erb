<div class="card">
  <div class="card-header" role="tab" id="headingOne">
    <h5 class="mb-0">
      <a data-toggle="collapse" data-parent="#accordion" href="#<%= control_id %>" aria-expanded="true" aria-controls="<%= control_id %>">
        <%= title %>
      </a>
    </h5>
  </div>

  <div id="<%= control_id %>" class="collapse show" role="tabpanel" aria-labelledby="headingOne">
    <div class="card-block">
      <% entries.each do |entry| %>
        <%= link_to entry.title, entry_path(entry.id) %><br>
      <% end %>
    </div>
  </div>
</div>