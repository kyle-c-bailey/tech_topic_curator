<% @phrases.each do |phrase| %>
  <%= "\"#{phrase.content}\" has #{phrase.count} occurrences" %>
  <% Item.where('lower(name) = ?', phrase.content.downcase).each do |item| %>
      <span style="color:red;"><%= item.category.name.titleize %></span>
  <% end %>
  <br>
  <% phrase.phrase_entries.each do |pe| %>
    <%= link_to pe.entry.title, entry_path(pe.entry_id) %><br>
    <% ContextWord.where('lower(name) LIKE (?)', pe.title.downcase).each do |word| %>
      <span style="color:red;"><%= word.context_category.name.titleize %></span>
    <% end %>
  <% end %>
  <br>
  <br>
<% end %>