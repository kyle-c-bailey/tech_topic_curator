<%# @word_frequencies.reverse.each do |word| %>
  <%# "\"#{word[0]}\" has #{word[1][:count]} occurrences" %>
  <%# word[1][:sources].to_s %>
<%# end %>

<% @phrases.each do |phrase| %>
  <%= "\"#{phrase.content}\" has #{phrase.count} occurrences" %>
  <%= phrase.phrase_entries.pluck(:entry_id).to_s %><br>
<% end %>